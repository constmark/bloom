[package]
name = "bloom-vendor-sdk-adapter-example"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1.0"
bloomai-core = { path = "../../crates/core" }
bloomai-backend = { path = "../../crates/backend" }
tracing = "0.1"
serde_json = "1.0"

[workspace]
# Independent template project

